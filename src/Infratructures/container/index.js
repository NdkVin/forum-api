/* istanbul ignore file */
const usersContainer = require('./usersContainer');
const authsContainer = require('./AuthsContainer');
const threadsContainer = require('./ThreadsContainer');
const commentContainer = require('./CommentContainer');

module.exports = {
  usersContainer,
  authsContainer,
  threadsContainer,
  commentContainer,
};
